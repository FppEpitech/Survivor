import { Config, Translation } from '../types';
export interface FileAction {
    path: string;
    type: 'new' | 'modified';
}
interface BuildTranslationOptions extends Required<Pick<Config, 'fileFormat'>>, Partial<Pick<Config, 'replace' | 'removeExtraKeys'>> {
    path: string;
    translation?: Translation;
}
export declare function buildTranslationFile({ path, translation, replace, removeExtraKeys, fileFormat, }: BuildTranslationOptions): FileAction;
export {};
