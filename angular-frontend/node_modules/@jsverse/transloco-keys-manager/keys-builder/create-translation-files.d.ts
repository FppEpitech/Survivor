import { Config, ScopeMap } from '../types';
export declare function createTranslationFiles({ scopeToKeys, langs, output, replace, removeExtraKeys, scopes, fileFormat, }: Config & {
    scopeToKeys: ScopeMap;
}): Promise<void>;
