import { tsquery } from '@phenomnomnominal/tsquery';
import { buildKeysFromASTNodes } from './build-keys-from-ast-nodes.js';
export function pureFunctionExtractor(ast) {
    const fns = tsquery(ast, `CallExpression Identifier[text=translate]`);
    return buildKeysFromASTNodes(fns);
}
//# sourceMappingURL=pure-function.extractor.js.map