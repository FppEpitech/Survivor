import { Config, ExtractionResult, ExtractorConfig, FileType, ScopeMap } from '../../types';
export declare function extractKeys({ input, scopes, defaultValue, files }: Config, fileType: FileType, extractor: (config: ExtractorConfig) => ScopeMap): ExtractionResult;
